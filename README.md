# GEES Website + Portal

This repository contains the structured GEES public website, role-based portal, Supabase backend setup files, and project documentation.

Current baseline: before Phase 11, with Supabase Auth/portal foundation and admin approval flow prepared.

## Important

Some media/PDF assets are represented by placeholder folders or `.gitkeep` files. Replace placeholders with real production assets before final deployment.

## Upload target

Deploy the contents of `public_html/` to your hosting `public_html` directory.
